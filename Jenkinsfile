pipeline {
    agent any

    stages {
        stage("Code-Check") {
            steps {
                git branch: 'main',
                url: 'https://github.com/Adhi-AWS/Azure-Angular.git'
            }
        }
    }
}
